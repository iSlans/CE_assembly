{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile a.out",
            "type": "shell",
            "options": {
                "cwd": "${fileDirname}"
            },
            "command": "g++ -g *.s *.h *.cpp ",
            "problemMatcher": [
                "$gcc"
            ],
            "presentation": {
                "reveal": "silent",
                // "close": true,
            }
        },
    ]
}
